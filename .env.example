PROJECT_NAME=Future ERP AI
ENV=dev
LOG_LEVEL=INFO

# Veritabanı (SQLite yerel gelistirme için)
DATABASE_URL=sqlite:///./future_erp_ai.db

# CORS (virgülle ayrılmış; React Vite varsayılanı)
# CORS_ORIGINS=http://127.0.0.1:5173,http://localhost:5173

# PostgreSQL (Faz 1+)
# DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/future_erp_ai

# AI (istege bagli — NLP / AI modulleri için)
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini
# GEMINI_API_KEY=
# GEMINI_MODEL=gemini-flash-latest
